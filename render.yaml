services:
  # Main web service - Flask backend + React frontend (pre-built)
  - type: web
    name: rompit-oe
    runtime: python
    plan: free
    buildCommand: |
      echo "Installing Python dependencies..."
      cd flask-backend
      pip install -r requirements.txt
    startCommand: cd flask-backend && gunicorn db_app:app
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_ETjaJbR48Ovl@ep-tiny-smoke-a4uifbri-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
